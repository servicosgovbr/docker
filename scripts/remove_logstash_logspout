set -x
set -e
set -o pipefail

cd '/root/docker'
git pull --rebase

docker-compose stop
docker-compose kill logstash logspout es1 es2 kibana
docker-compose rm -f logstash logspout es1 es2 kibana

docker-compose up -d
docker-compose restart balanceador
